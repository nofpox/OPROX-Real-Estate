# OPROX PROPERTIES — UNIFIED GITHUB MONOREPO PREPARATION

We have now confirmed the intended architecture.

OPROX Properties is ONE unified product.

The Web Portal, Housin Mobile application, Real Estate API, database layer, and shared libraries MUST NOT be treated as separate independent projects.

The ORIGINAL REPLIT workspace is the source that contains the complete system.

## TARGET ARCHITECTURE

We want ONE GitHub repository containing the ENTIRE OPROX Properties Replit project from the repository/workspace root.

Expected structure should include, where currently present:

```text
OPROX-PROPERTIES/
│
├── artifacts/
│   ├── rkz/                 # Housin Mobile — Expo / React Native
│   ├── real-estate/         # Web / Real Estate Portal
│   └── realestate-api/      # Express Real Estate API
│
├── lib/
│   ├── db/                  # PostgreSQL / Drizzle
│   ├── api-spec/
│   ├── api-client-react/
│   ├── api-zod/
│   └── other shared libraries
│
├── scripts/
├── docs/
├── package.json
├── lockfile
└── all other required project files
```

The exact existing Replit structure takes priority over this example.

DO NOT artificially reorganize the project just to match the example.

## CRITICAL REQUIREMENT

DO NOT upload only the Mobile application.

DO NOT upload only the Web application.

DO NOT create one GitHub repository for Mobile and another for Web.

DO NOT separate the API.

DO NOT separate the database/shared libraries.

The GitHub repository must represent the COMPLETE ORIGINAL REPLIT WORKSPACE as ONE MONOREPO.

The relationship must remain:

```text
             OPROX PROPERTIES
                    │
       ┌────────────┴────────────┐
       │                         │
 Web / Portal              Housin Mobile
       │                  artifacts/rkz
       │                         │
       └──────────┬──────────────┘
                  │
          realestate-api
                  │
        PostgreSQL / Drizzle
```

Mobile and Web are two clients of the SAME OPROX Properties system.

The Mobile application currently consumes endpoints such as:

GET /realestate-api/listings

GET /realestate-api/listings/:id

These relationships MUST be preserved.

---

# PHASE 1 — VERIFY BEFORE UPLOAD

Before performing ANY GitHub upload, inspect the current ORIGINAL Replit workspace.

Confirm that you can locate:

1. Housin Mobile source
2. Web / Real Estate Portal source
3. realestate-api source
4. PostgreSQL / Drizzle schemas
5. shared libraries
6. required root configuration
7. package manifests / workspace configuration
8. all source code required for the complete system

Specifically verify whether these paths exist:

* artifacts/rkz
* artifacts/real-estate
* artifacts/realestate-api
* lib/db
* lib/api-spec
* lib/api-client-react
* lib/api-zod

If actual names differ, report the correct paths.

---

# PHASE 2 — PROTECT THE ORIGINAL

DO NOT:

* delete any existing source
* clean the workspace
* remove artifacts
* remove archives
* restructure the monorepo
* reset Git
* overwrite history
* delete existing GitHub repositories
* delete branches
* force push
* expose secrets
* upload .env secrets
* upload API keys
* upload credentials

Ensure `.gitignore` safely excludes secrets and generated/unnecessary files before any future upload.

Do NOT modify `.gitignore` yet during this verification phase. Report problems first.

---

# PHASE 3 — UNIFIED GITHUB DESTINATION

The final objective is ONE new clean GitHub repository for the complete product.

Recommended repository identity:

OPROX-PROPERTIES

But DO NOT create it yet unless explicit approval has already been given.

The repository must receive the project from the REPLIT WORKSPACE ROOT.

Not:

`artifacts/rkz` only.

Not:

`artifacts/real-estate` only.

Instead:

THE COMPLETE PROJECT ROOT.

This ensures Mobile + Web + API + DB + shared libraries remain together and versioned together.

---

# EXISTING OLD GITHUB REPOSITORIES

DO NOT DELETE THEM.

DO NOT MODIFY THEM.

DO NOT FORCE PUSH OVER THEM.

They are temporary safety backups.

After the unified repository has been successfully uploaded, verified, cloned/tested, and confirmed complete, we will decide separately whether to archive or delete the old repositories.

---

# REQUIRED RESPONSE BEFORE EXECUTION

Do NOT perform the GitHub migration yet.

Return:

1. Replit workspace root
2. Mobile source path
3. Web source path
4. API source path
5. Database source path
6. Shared libraries found
7. Root package/workspace configuration
8. Files/directories that must be excluded from GitHub
9. Secret exposure risks
10. Existing Git status
11. Existing GitHub remote, if any
12. Confirmation that the complete project can be uploaded as ONE monorepo
13. Proposed unified repository structure
14. Any blockers before upload

Finish with exactly:

**OPROX PROPERTIES UNIFIED MONOREPO READY FOR REVIEW — NO MIGRATION PERFORMED YET**

Wait for approval before creating, pushing, deleting, restructuring, or migrating anything.
